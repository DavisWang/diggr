{
  "name": "diggr",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:sprites": "node scripts/generateSpriteSheets.mjs",
    "generate:assets": "npm run generate:sprites && python3 scripts/generate_upgrade_shop_icons.py && python3 scripts/generate_consumable_shop_icons.py && python3 scripts/generate_refinery_shop_icons.py",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  }
}
